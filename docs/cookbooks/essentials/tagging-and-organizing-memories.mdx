---
title: Tag and Organize Memories
description: "Let Mem0 auto-categorize support data so teams retrieve the right facts fast."
---

> 中文注释：本文件非代码内容已翻译为中文，代码块与配置示例保持原样。



当您拥有大量内存数据时，在后处理期间对其进行排序会变得很困难。如果您的内存存储了解无需付出太多努力即可创建标签和存储桶的重要性，该怎么办？

Mem0 通过提供使用自定义类别组织内存的灵活性来为您处理此问题。本食谱向您展示如何为客户支持平台标记和组织记忆。

 - -

＃＃ 设置```python
from mem0 import MemoryClient

client = MemoryClient(api_key="your-api-key")
```
<注意>
在添加内存之前，使用“client.project.update()”在**项目级别**定义自定义类别。类别适用于所有未来的记忆 - Mem0 根据内容语义自动分配它们。
</注>

---

## 问题

如果没有类别，所有的记忆都集中在一个无差别的桶中。支持代理浪费时间搜索所有内容以查找计费问题、帐户详细信息或过去的票证。

让我们看看如果没有组织会发生什么：```python
# Joseph (support agent) stores various customer interactions
client.add(
    "Maria called about her account password reset",
    user_id="maria",
)

client.add(
    "Maria was charged twice for last month's subscription",
    user_id="maria",
)

client.add(
    "Maria wants to upgrade to the premium plan",
    user_id="maria",
)

# Now try to find just billing issues
all_memories = client.get_all(filters={"user_id": "maria"})
print(f"Total memories: {len(all_memories['results'])}")

for memory in all_memories['results']:
    print(f"- {memory['memory']}")

```
**输出：**```
Total memories: 3
- Maria called about her account password reset
- Maria was charged twice for last month's subscription
- Maria wants to upgrade to the premium plan

```
<警告>
如果没有类别，代理就会浪费时间阅读所有内容。对于拥有 100 条记忆的客户来说，找到一个帐单问题意味着扫描所有 100 个帐单问题。类别可让您准确筛选出所需的内容 — 仅帐单问题，不涉及密码重置或混合反馈。
</警告>

一切都混合在一起。支持人员必须通读所有记忆才能找到他们需要的内容。

---

## 自定义类别

定义与您的支持团队对客户问题的看法相匹配的类别：```python
custom_categories = [
    {"support_tickets": "Customer issues and resolutions"},
    {"account_info": "Account details and preferences"},
    {"billing": "Payment history and billing questions"},
    {"product_feedback": "Feature requests and feedback"},
]

client.project.update(custom_categories=custom_categories)

```
<提示>
从 3-5 个符合您团队想法的明确类别开始。太多的类别会降低自动标记的准确性。如果需要的话稍后添加更多 - 扩展比修复过于复杂的分类更容易。
</提示>

这些类别现在在整个项目范围内可用。每个记忆都可以标记一个或多个类别。

---

## 标记记忆

一旦在项目级别定义了类别，Mem0 就会根据内容自动分配它们：```python
# Billing issue - automatically tagged as "billing"
client.add(
    "Maria was charged twice for last month's subscription",
    user_id="maria",
    metadata={"priority": "high", "source": "phone_call"}
)

# Account update - automatically tagged as "account_info"
client.add(
    "Maria changed her email to maria.new@example.com",
    user_id="maria",
    metadata={"source": "web_portal"}
)

# Product feedback - automatically tagged as "product_feedback"
client.add(
    "Maria requested a dark mode feature for the dashboard",
    user_id="maria",
    metadata={"source": "chat"}
)

```
Mem0 读取内容并智能地分配适当的类别。您无需手动标记 - 平台会根据类别定义为您完成标记。

---

## 按类别检索

按类别过滤记忆以准确找到您需要的内容：```python
# Joseph needs to pull all billing issues for audit
billing_issues = client.get_all(
    filters={
        "AND": [
            {"user_id": "maria"},
            {"categories": {"in": ["billing"]}}
        ]
    }
)

print("Billing issues:")
for memory in billing_issues['results']:
    print(f"- {memory['memory']}")

```
**输出：**```
Billing issues:
- Maria was charged twice for last month's subscription

```
<信息图标=“检查”>
**预期输出：** 仅返回计费问题 - 没有密码重置，没有升级请求。类别过滤有效。 Joseph 无需阅读不相关的支持票证即可审核账单。
</信息>

仅返回与计费相关的内存。无需过滤帐户更新或反馈。

您还可以检索多个类别：```python
# Get both account info and billing
account_and_billing = client.get_all(
    filters={
        "AND": [
            {"user_id": "maria"},
            {"categories": {"in": ["account_info", "billing"]}}
        ]
    }
)

for memory in account_and_billing['results']:
    print(f"[{memory['categories'][0]}] {memory['memory']}")

```
**输出：**```
[account_info] Maria changed her email to maria.new@example.com
[billing] Maria was charged twice for last month's subscription

```
---

## 更新类别

根据内容自动分配类别。要触发重新分类，请更新内存内容：```python
# Find memories that need re-categorization
needs_update = client.get_all(
    filters={
        "AND": [
            {"user_id": "maria"},
            {"categories": {"in": ["misc"]}}
        ]
    }
)

# Update memory content to trigger re-categorization
for memory in needs_update['results']:
    client.update(
        memory_id=memory['id'],
        data=memory['memory']  # Re-process with current category definitions
    )

```
当您更新内存时，Mem0 会根据您当前的类别定义重新分析它。当您引入新类别或完善类别描述时，这非常有用。

---

## 你构建了什么

具有智能内存组织的客户支持平台：

- **项目范围的类别** - 支持票证、账单、帐户信息和产品反馈自动分类
- **自动标记** - Mem0 根据内容语义分配类别，无需手动标记
- **过滤检索** - 使用“类别：{in：[...]}”仅提取账单问题或仅提取帐户更新
- **重新分类** - 更新内存内容以触发针对新类别定义的重新分析
- **多类别支持** - 适当时记忆可以属于多个类别

此模式可以从 10 个客户扩展到 10,000 个客户，而不会降低检索速度。---

## 总结

类别使检索更快，合规性更容易。使用 `client.project.update()` 定义 3-5 个明确的类别，让 Mem0 根据内容自动分配它们，然后使用 `categories: {in: [...]}` 进行过滤以准确提取您需要的内容。客服人员无需搜索所有内容，而是直接跳转到他们需要的信息类型——账单问题、帐户详细信息或支持票证。

<卡组列={2}>
  <Card title="控制内存摄入" icon="filter" href="/cookbooks/essentials/controlling-memory-ingestion">
    在噪音进入存储之前过滤噪音，使类别保持有意义。
  </卡>
  <Card title="导出标记记忆" icon="download" href="/cookbooks/essentials/exporting-memories">
    使用类别来推动审核、迁移和合规性报告。
  </卡>
</卡组>
