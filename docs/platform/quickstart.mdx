---
title: Quickstart
description: "Set up your Mem0 Platform account, install the SDK, and store your first memory in under five minutes."
icon: "bolt"
iconType: "solid"
---

> 中文注释：本文件非代码内容已翻译为中文，代码块与配置示例保持原样。



5 分钟内即可开始使用 Mem0 平台的托管 API。本指南向您展示如何验证和存储您的第一个记忆。

## 先决条件

- Mem0 平台帐户（<a href="https://app.mem0.ai?utm_source=oss&utm_medium=platform-quickstart" rel="nofollow">在此注册</a>）
- API 密钥（<a href="https://app.mem0.ai/dashboard/settings?tab=api-keys&subtab=configuration" rel="nofollow">从仪表板获取一个</a>）
- Python 3.10+、Node.js 14+ 或 cURL

## 安装

<步骤>
<步骤标题=“安装SDK”>
<代码组>```bash pip
pip install mem0ai
```

```bash npm
npm install mem0ai
```
</代码组>
</步骤>

<Step title="设置您的 API 密钥">
<代码组>```python Python
from mem0 import MemoryClient

client = MemoryClient(api_key="your-api-key")
````

```javascript JavaScript
import MemoryClient from 'mem0ai';
const client = new MemoryClient({ apiKey: 'your-api-key' });
````

```bash cURL
export MEM0_API_KEY="your-api-key"
```

```bash CLI
mem0 init --api-key "your-api-key"
```
</代码组>
</步骤>

<Step title="添加内存">
<代码组>```python Python
messages = [
    {"role": "user", "content": "I'm a vegetarian and allergic to nuts."},
    {"role": "assistant", "content": "Got it! I'll remember your dietary preferences."}
]
client.add(messages, user_id="user123")
````

```javascript JavaScript
const messages = [
    {"role": "user", "content": "I'm a vegetarian and allergic to nuts."},
    {"role": "assistant", "content": "Got it! I'll remember your dietary preferences."}
];
await client.add(messages, { userId: "user123" });
````

```bash cURL
curl -X POST https://api.mem0.ai/v3/memories/add/ \
  -H "Authorization: Token $MEM0_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "messages": [
      {"role": "user", "content": "Im a vegetarian and allergic to nuts."},
      {"role": "assistant", "content": "Got it! Ill remember your dietary preferences."}
    ],
    "user_id": "user123"
  }'
```

```bash CLI
mem0 add "I'm a vegetarian and allergic to nuts." --user-id user123
```
</代码组>
</步骤>

<步骤标题=“搜索记忆”>
<代码组>```python Python
results = client.search("What are my dietary restrictions?", filters={"user_id": "user123"})
print(results)
````

```javascript JavaScript
const results = await client.search("What are my dietary restrictions?", { filters: { user_id: "user123" } });
console.log(results);
````

```bash cURL
curl -X POST https://api.mem0.ai/v3/memories/search/ \
  -H "Authorization: Token $MEM0_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "What are my dietary restrictions?",
    "filters": {"user_id": "user123"}
  }'
```

```bash CLI
mem0 search "What are my dietary restrictions?" --user-id user123
```
</代码组>

**输出：**```json
{
  "results": [
    {
      "id": "14e1b28a-2014-40ad-ac42-69c9ef42193d",
      "memory": "Allergic to nuts",
      "user_id": "user123",
      "categories": ["health"],
      "created_at": "2025-10-22T04:40:22.864647-07:00",
      "score": 0.30
    }
  ]
}
```
</步骤>
</步骤>

<Callout type="tip" icon="plug">
  **专业提示**：希望人工智能代理自动管理自己的内存吗？使用 <Link href="/platform/mem0-mcp">Mem0 MCP</Link> 让 LLM 决定何时保存、搜索和更新内存。
</标注>

## 下一步是什么？<卡组列={3}>
<Card title="内存操作" icon="database" href="/core-concepts/memory-operations/add">
了解如何通过完整的 CRUD 操作来搜索、更新和删除内存
</卡>

<Card title="平台功能" icon="star" href="/platform/features/platform-overview">
  探索元数据过滤、图形内存和 Webhooks 等高级功能
</卡>

<Card title="API 参考" icon="code" href="/api-reference/memory/add-memories">
查看完整的 API 文档和集成示例
</卡>
</卡组>

## 其他资源

- **[平台与 OSS](/platform/platform-vs-oss)** - 了解平台和开源之间的差异
- **[疑难解答](/platform/faqs)** - 常见问题和解决方案
- **[集成示例](/cookbooks/companions/quickstart-demo)** - 查看 Mem0 的实际应用
