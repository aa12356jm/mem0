---
title: "LM Studio"
description: "Configure LM Studio as an embedding provider in Mem0 for local embedding generation with models like nomic-embed-text."
---

> 中文注释：本文件非代码内容已翻译为中文，代码块与配置示例保持原样。



您可以使用 LM Studio 中的嵌入模型在本地运行 Mem0。

＃＃＃ 用法```python
import os
from mem0 import Memory

os.environ["OPENAI_API_KEY"] = "your_api_key" # For LLM

config = {
    "embedder": {
        "provider": "lmstudio",
        "config": {
            "model": "nomic-embed-text-v1.5-GGUF/nomic-embed-text-v1.5.f16.gguf"
        }
    }
}

m = Memory.from_config(config)
messages = [
    {"role": "user", "content": "I'm planning to watch a movie tonight. Any recommendations?"},
    {"role": "assistant", "content": "How about thriller movies? They can be quite engaging."},
    {"role": "user", "content": "I’m not a big fan of thriller movies but I love sci-fi movies."},
    {"role": "assistant", "content": "Got it! I'll avoid thriller recommendations and suggest sci-fi movies in the future."}
]
m.add(messages, user_id="john")
```
### 配置

以下是可用于配置 LM Studio 嵌入器的参数：

|参数|描述 |默认值 |
| ---| ---| ---|
| `模型` |要使用的 LM Studio 模型的名称 | `nomic-embed-text-v1.5-GGUF/nomic-embed-text-v1.5.f16.gguf` |
| `embedding_dims` |嵌入模型的尺寸| `1536` |
| `lmstudio_base_url` | LM Studio 连接的基本 URL | `http://localhost:1234/v1` |
