---
title: xAI
description: "Configure xAI Grok models as an LLM provider in Mem0 with API key setup and usage examples."
---

> 中文注释：本文件非代码内容已翻译为中文，代码块与配置示例保持原样。



[xAI](https://x.ai/) 是埃隆·马斯克创立的一家新的人工智能公司，开发大型语言模型，包括 Grok。 Grok 接受了来自 X（以前称为 Twitter）的实时数据的培训，旨在提供准确、最新且充满智慧和幽默的回复。

为了使用 xAI 的法学硕士，请访问他们的[平台](https://console.x.ai) 并获取 API 密钥。将 API 密钥设置为“XAI_API_KEY”环境变量以使用下面示例中给出的模型。

＃＃ 用法```python
import os
from mem0 import Memory

os.environ["OPENAI_API_KEY"] = "your-api-key" # used for embedding model
os.environ["XAI_API_KEY"] = "your-api-key"

config = {
    "llm": {
        "provider": "xai",
        "config": {
            "model": "grok-3-beta",
            "temperature": 0.1,
            "max_tokens": 2000,
        }
    }
}

m = Memory.from_config(config)
messages = [
    {"role": "user", "content": "I'm planning to watch a movie tonight. Any recommendations?"},
    {"role": "assistant", "content": "How about thriller movies? They can be quite engaging."},
    {"role": "user", "content": "I’m not a big fan of thriller movies but I love sci-fi movies."},
    {"role": "assistant", "content": "Got it! I'll avoid thriller recommendations and suggest sci-fi movies in the future."}
]
m.add(messages, user_id="alice", metadata={"category": "movies"})
```
## 配置

`xai` 配置的所有可用参数都存在于 [配置中所有参数的主列表](../config) 中。
