---
title: Azure OpenAI
description: "Configure Azure OpenAI as an LLM provider in Mem0 with Azure Identity authentication and deployment settings."
---

> 中文注释：本文件非代码内容已翻译为中文，代码块与配置示例保持原样。



<Note> Mem0 现在支持 TypeScript SDK 中的 Azure OpenAI 模型 </Note>

要使用 Azure OpenAI 模型，必须设置“LLM_AZURE_OPENAI_API_KEY”、“LLM_AZURE_ENDPOINT”、“LLM_AZURE_DEPLOYMENT”和“LLM_AZURE_API_VERSION”环境变量。您可以从[Azure](https://azure.microsoft.com/)获取Azure API密钥。

或者，您可以使用 Azure Identity 通过 Azure OpenAI 进行身份验证，这允许您使用托管标识或服务主体进行生产，使用 Azure CLI 登录进行开发，而不是使用 API 密钥。如果要使用 Azure 身份，***不要***在配置字典中设置“LLM_AZURE_OPENAI_API_KEY”环境变量或 api_key。> **注意**：推理模型目前不支持以下内容`并行工具调用`、`温度`、`top_p`、`presence_penalty`、`frequity_penalty`、`logprobs`、`top_logprobs`、`logit_bias`、`max_tokens`


## 用法

<代码组>```python Python
import os
from mem0 import Memory

os.environ["OPENAI_API_KEY"] = "your-api-key" # used for embedding model

os.environ["LLM_AZURE_OPENAI_API_KEY"] = "your-api-key"
os.environ["LLM_AZURE_DEPLOYMENT"] = "your-deployment-name"
os.environ["LLM_AZURE_ENDPOINT"] = "your-api-base-url"
os.environ["LLM_AZURE_API_VERSION"] = "version-to-use"

config = {
    "llm": {
        "provider": "azure_openai",
        "config": {
            "model": "your-deployment-name",
            "temperature": 0.1,
            "max_tokens": 2000,
            "azure_kwargs": {
                  "azure_deployment": "",
                  "api_version": "",
                  "azure_endpoint": "",
                  "api_key": "",
                  "default_headers": {
                    "CustomHeader": "your-custom-header",
                  }
              }
        }
    }
}

m = Memory.from_config(config)
messages = [
    {"role": "user", "content": "I'm planning to watch a movie tonight. Any recommendations?"},
    {"role": "assistant", "content": "How about thriller movies? They can be quite engaging."},
    {"role": "user", "content": "I’m not a big fan of thriller movies but I love sci-fi movies."},
    {"role": "assistant", "content": "Got it! I'll avoid thriller recommendations and suggest sci-fi movies in the future."}
]
m.add(messages, user_id="alice", metadata={"category": "movies"})
```

```typescript TypeScript
import { Memory } from 'mem0ai/oss';

const config = {
  llm: {
    provider: 'azure_openai',
    config: {
      apiKey: process.env.AZURE_OPENAI_API_KEY || '',
      modelProperties: {
        endpoint: 'https://your-api-base-url',
        deployment: 'your-deployment-name',
        modelName: 'your-model-name',
        apiVersion: 'version-to-use',
        // Any other parameters you want to pass to the Azure OpenAI API
      },
    },
  },
};

const memory = new Memory(config);
const messages = [
    {"role": "user", "content": "I'm planning to watch a movie tonight. Any recommendations?"},
    {"role": "assistant", "content": "How about thriller movies? They can be quite engaging."},
    {"role": "user", "content": "I’m not a big fan of thriller movies but I love sci-fi movies."},
    {"role": "assistant", "content": "Got it! I'll avoid thriller recommendations and suggest sci-fi movies in the future."}
]
await memory.add(messages, { userId: "alice", metadata: { category: "movies" } });
```
</代码组>


我们还支持新的 [OpenAI 结构化输出](https://platform.openai.com/docs/guides/structed-outputs/introduction) 模型。 Typescript SDK 尚不支持“azure_openai_structured”模型。```python
import os
from mem0 import Memory

os.environ["LLM_AZURE_OPENAI_API_KEY"] = "your-api-key"
os.environ["LLM_AZURE_DEPLOYMENT"] = "your-deployment-name"
os.environ["LLM_AZURE_ENDPOINT"] = "your-api-base-url"
os.environ["LLM_AZURE_API_VERSION"] = "version-to-use"

config = {
    "llm": {
        "provider": "azure_openai_structured",
        "config": {
            "model": "your-deployment-name",
            "temperature": 0.1,
            "max_tokens": 2000,
            "azure_kwargs": {
                  "azure_deployment": "",
                  "api_version": "",
                  "azure_endpoint": "",
                  "api_key": "",
                  "default_headers": {
                    "CustomHeader": "your-custom-header",
                  }
              }
        }
    }
}
```
作为使用 API 密钥的替代方法，Azure Identity 凭据链可用于通过 [Azure OpenAI 基于角色的安全性](https://learn.microsoft.com/en-us/azure/ai-foundry/openai/how-to/role-based-access-control) 进行身份验证。 

<Note> 如果提供了 API 密钥，它将用于通过 Azure Identity 进行身份验证 </Note>

以下是将 Mem0 与 Azure OpenAI 和 Azure Identity 结合使用的示例配置：```python
import os
from mem0 import Memory
# You can set the values directly in the config dictionary or use environment variables

os.environ["LLM_AZURE_DEPLOYMENT"] = "your-deployment-name"
os.environ["LLM_AZURE_ENDPOINT"] = "your-api-base-url"
os.environ["LLM_AZURE_API_VERSION"] = "version-to-use"

config = {
    "llm": {
        "provider": "azure_openai_structured",
        "config": {
            "model": "your-deployment-name",
            "temperature": 0.1,
            "max_tokens": 2000,
            "azure_kwargs": {
                  "azure_deployment": "<your-deployment-name>",
                  "api_version": "<version-to-use>",
                  "azure_endpoint": "<your-api-base-url>",
                  "default_headers": {
                    "CustomHeader": "your-custom-header",
                  }
              }
        }
    }
}
```
请参阅[Azure Identity 故障排除提示](https://github.com/Azure/azure-sdk-for-python/blob/main/sdk/identity/azure-identity/TROUBLESHOOTING.md#troubleshoot-environmentcredential-authentication-issues) 设置 Azure Identity 凭据。


## 配置

`azure_openai` 配置的所有可用参数都存在于 [配置中所有参数的主列表](../config) 中。
