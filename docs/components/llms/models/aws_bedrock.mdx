---
title: AWS Bedrock
description: "Configure AWS Bedrock as an LLM provider in Mem0 with IAM authentication and Claude model support."
---

> 中文注释：本文件非代码内容已翻译为中文，代码块与配置示例保持原样。



### 设置
- 在使用 AWS Bedrock LLM 之前，请确保您拥有 [Bedrock 控制台](https://us-east-1.console.aws.amazon.com/bedrock/home?region=us-east-1#/modelaccess) 的适当模型访问权限。
- 您还需要使用 [AWS 文档](https://boto3.amazonaws.com/v1/documentation/api/latest/guide/credentials.html#configuring-credentials) 中的方法对“boto3”客户端进行身份验证
- 您必须导出“AWS_REGION”、“AWS_ACCESS_KEY”和“AWS_SECRET_ACCESS_KEY”才能设置环境变量。

＃＃＃ 用法```python
import os
from mem0 import Memory

os.environ['AWS_REGION'] = 'us-west-2'
os.environ["AWS_ACCESS_KEY_ID"] = "xx"
os.environ["AWS_SECRET_ACCESS_KEY"] = "xx"

config = {
    "llm": {
        "provider": "aws_bedrock",
        "config": {
            "model": "anthropic.claude-3-5-haiku-20241022-v1:0",
            "temperature": 0.2,
            "max_tokens": 2000,
        }
    }
}

m = Memory.from_config(config)
messages = [
    {"role": "user", "content": "I'm planning to watch a movie tonight. Any recommendations?"},
    {"role": "assistant", "content": "How about thriller movies? They can be quite engaging."},
    {"role": "user", "content": "I’m not a big fan of thriller movies but I love sci-fi movies."},
    {"role": "assistant", "content": "Got it! I'll avoid thriller recommendations and suggest sci-fi movies in the future."}
]
m.add(messages, user_id="alice", metadata={"category": "movies"})
```
### 配置

`aws_bedrock` 配置的所有可用参数都存在于 [配置中所有参数的主列表](../config) 中。
