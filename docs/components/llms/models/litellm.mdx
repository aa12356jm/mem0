---
title: "LiteLLM"
description: "Use LiteLLM as an LLM provider in Mem0 to access over 100 language models through a unified interface."
---

> 中文注释：本文件非代码内容已翻译为中文，代码块与配置示例保持原样。



[Litellm](https://litellm.vercel.app/docs/) 与 100 多种大型语言模型 (LLM) 兼容，全部使用标准化的输入/输出格式。您可以探索与 Litellm 一起使用的[可用模型](https://litellm.vercel.app/docs/providers)。确保为您选择使用的模型设置“API_KEY”。

＃＃ 用法```python
import os
from mem0 import Memory

os.environ["OPENAI_API_KEY"] = "your-api-key"

config = {
    "llm": {
        "provider": "litellm",
        "config": {
            "model": "gpt-5-mini",
            "temperature": 0.2,
            "max_tokens": 2000,
        }
    }
}

m = Memory.from_config(config)
messages = [
    {"role": "user", "content": "I'm planning to watch a movie tonight. Any recommendations?"},
    {"role": "assistant", "content": "How about thriller movies? They can be quite engaging."},
    {"role": "user", "content": "I’m not a big fan of thriller movies but I love sci-fi movies."},
    {"role": "assistant", "content": "Got it! I'll avoid thriller recommendations and suggest sci-fi movies in the future."}
]
m.add(messages, user_id="alice", metadata={"category": "movies"})
```
## 配置

`litellm` 配置的所有可用参数都存在于 [配置中所有参数的主列表](../config) 中。
