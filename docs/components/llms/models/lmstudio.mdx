---
title: LM Studio
description: "Configure LM Studio as an LLM provider in Mem0 for running local language models via an OpenAI-compatible API."
---

> 中文注释：本文件非代码内容已翻译为中文，代码块与配置示例保持原样。



要将 LM Studio 与 Mem0 结合使用，您需要让 LM Studio 在本地运行并启用其服务器。 LM Studio 提供了一种使用 OpenAI 兼容 API 运行本地 LLM 的方法。

## 用法

<代码组>```python Python
import os
from mem0 import Memory

os.environ["OPENAI_API_KEY"] = "your-api-key" # used for embedding model

config = {
    "llm": {
        "provider": "lmstudio",
        "config": {
            "model": "lmstudio-community/Meta-Llama-3.1-70B-Instruct-GGUF/Meta-Llama-3.1-70B-Instruct-IQ2_M.gguf",
            "temperature": 0.2,
            "max_tokens": 2000,
            "lmstudio_base_url": "http://localhost:1234/v1", # default LM Studio API URL
            "lmstudio_response_format": {"type": "json_schema", "json_schema": {"type": "object", "schema": {}}},
        }
    }
}

m = Memory.from_config(config)
messages = [
    {"role": "user", "content": "I'm planning to watch a movie tonight. Any recommendations?"},
    {"role": "assistant", "content": "How about thriller movies? They can be quite engaging."},
    {"role": "user", "content": "I'm not a big fan of thriller movies but I love sci-fi movies."},
    {"role": "assistant", "content": "Got it! I'll avoid thriller recommendations and suggest sci-fi movies in the future."}
]
m.add(messages, user_id="alice", metadata={"category": "movies"})
```
</代码组>

### 完全本地运行

您还可以使用 LM Studio 进行 LLM 和嵌入，以完全在本地运行 Mem0：```python
from mem0 import Memory

# No external API keys needed!
config = {
    "llm": {
        "provider": "lmstudio"
    },
    "embedder": {
        "provider": "lmstudio"
    }
}

m = Memory.from_config(config)
messages = [
    {"role": "user", "content": "I'm planning to watch a movie tonight. Any recommendations?"},
    {"role": "assistant", "content": "How about thriller movies? They can be quite engaging."},
    {"role": "user", "content": "I'm not a big fan of thriller movies but I love sci-fi movies."},
    {"role": "assistant", "content": "Got it! I'll avoid thriller recommendations and suggest sci-fi movies in the future."}
]
m.add(messages, user_id="alice123", metadata={"category": "movies"})
```
<注意>
  当使用 LM Studio 进行 LLM 和嵌入时，请确保您拥有：
  1. 加载用于生成响应的 LLM 模型
  2. 为向量嵌入加载的嵌入模型
  3. 服务器启用可访问的正确端点
</注>

<注意>
  要使用 LM Studio，您需要：
  1.下载并安装【LM Studio】(https://lmstudio.ai/)
  2. 从“服务器”选项卡启动本地服务器
  3. 在配置中设置适当的 `lmstudio_base_url` （默认值通常是 http://localhost:1234/v1）
</注>

## 配置

`lmstudio` 配置的所有可用参数都存在于 [配置中所有参数的主列表](../config) 中。
