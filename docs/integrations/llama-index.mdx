---
title: LlamaIndex
description: "Use Mem0 as a memory store in LlamaIndex with support for ReAct and FunctionCalling agent types."
---

> 中文注释：本文件非代码内容已翻译为中文，代码块与配置示例保持原样。



LlamaIndex 支持 Mem0 作为[内存存储](https://llamahub.ai/l/memory/llama-index-memory-mem0)。在本指南中，我们将向您展示如何使用它。

<注释类型=“信息”>
  [**Mem0Memory**](https://docs.llamaindex.ai/en/stable/examples/memory/Mem0Memory/) 现在支持 **ReAct** 和 **FunctionCalling** 代理。
</注>

### 安装

要安装所需的包，请运行：```bash
pip install llama-index-core llama-index-memory-mem0 python-dotenv
```
### 使用 Mem0 平台进行设置

将 Mem0 平台 API 密钥设置为环境变量。您可以将 `<your-mem0-api-key>` 替换为您的实际 API 密钥：

<注释类型=“信息”>
  您可以从 <a href="https://app.mem0.ai/login?utm_source=oss&utm_medium=integration-llama-index" rel="nofollow">Mem0 平台</a>获取 Mem0 平台 API 密钥。
</注>```python
from dotenv import load_dotenv
import os

load_dotenv()

# os.environ["MEM0_API_KEY"] = "<your-mem0-api-key>"
```
导入必要的模块并创建 Mem0Memory 实例：```python
from llama_index.memory.mem0 import Mem0Memory

context = {"user_id": "alice"}
memory_from_client = Mem0Memory.from_client(
    context=context,
    search_msg_limit=4,  # optional, default is 5
)
```
上下文用于识别 Mem0 中的用户、代理或对话。需要在 `Mem0Memory` 构造函数中至少传递一个字段。它可以是以下任何一个：```python
context = {
    "user_id": "alice", 
    "agent_id": "llama_agent_1",
    "run_id": "run_1",
}
```
`search_msg_limit` 是可选的，默认为 5。它是用于从 Mem0 进行内存检索的聊天历史记录中的消息数。更多数量的消息将导致更多的上下文用于检索，但也会增加检索时间，并可能导致一些不需要的结果。

<注释类型=“信息”>
  `search_msg_limit` 与 `limit` 不同。 “limit”是从 Mem0 检索的消息数量，用于搜索。
</注>

### 使用 Mem0 OSS 设置

通过提供配置详细信息来设置 Mem0 OSS：

<注释类型=“信息”>
  要了解有关 Mem0 OSS 的更多信息，请阅读 [Mem0 OSS 快速入门](https://docs.mem0.ai/open-source/overview)。
</注>```python
config = {
    "vector_store": {
        "provider": "qdrant",
        "config": {
            "collection_name": "test_9",
            "host": "localhost",
            "port": 6333,
            "embedding_model_dims": 1536,  # Change this according to your local model's dimensions
        },
    },
    "llm": {
        "provider": "openai",
        "config": {
            "model": "gpt-5-mini",
            "temperature": 0.2,
            "max_tokens": 2000,
        },
    },
    "embedder": {
        "provider": "openai",
        "config": {"model": "text-embedding-3-small"},
    },
}
```
创建Mem0Memory实例：```python
memory_from_config = Mem0Memory.from_config(
    context=context,
    config=config,
    search_msg_limit=4,  # optional, default is 5
    # Remove deprecation warnings
)
```
初始化LLM```python
from llama_index.llms.openai import OpenAI
from dotenv import load_dotenv

load_dotenv()

# os.environ["OPENAI_API_KEY"] = "<your-openai-api-key>"
llm = OpenAI(model="gpt-5-mini")
```
### SimpleChatEngine
使用“SimpleChatEngine”通过内存开始与客服人员聊天。```python
from llama_index.core.chat_engine import SimpleChatEngine

agent = SimpleChatEngine.from_defaults(
    llm=llm, memory=memory_from_client  # or memory_from_config
)

# Start the chat
response = agent.chat("Hi, My name is Alice")
print(response)
```
现在我们将学习如何将 Mem0 与 FunctionCalling 和 ReAct 代理一起使用。

初始化工具：```python
from llama_index.core.tools import FunctionTool


def call_fn(name: str):
    """Call the provided name.
    Args:
        name: str (Name of the person)
    """
    print(f"Calling... {name}")


def email_fn(name: str):
    """Email the provided name.
    Args:
        name: str (Name of the person)
    """
    print(f"Emailing... {name}")


call_tool = FunctionTool.from_defaults(fn=call_fn)
email_tool = FunctionTool.from_defaults(fn=email_fn)
```
### 函数调用代理```python
from llama_index.core.agent import FunctionCallingAgent

agent = FunctionCallingAgent.from_tools(
    [call_tool, email_tool],
    llm=llm,
    memory=memory_from_client,  # or memory_from_config
    verbose=True,
)

# Start the chat
response = agent.chat("Hi, My name is Alice")
print(response)
```
### 反应代理```python
from llama_index.core.agent import ReActAgent

agent = ReActAgent.from_tools(
    [call_tool, email_tool],
    llm=llm,
    memory=memory_from_client,  # or memory_from_config
    verbose=True,
)

# Start the chat
response = agent.chat("Hi, My name is Alice")
print(response)
```
## 主要特点

1. **内存集成**：使用 Mem0 存储和检索过去交互的相关信息。
2. **个性化**：根据用户历史记录和偏好提供上下文感知代理响应。
3. **灵活的架构**：LlamaIndex 允许轻松地将内存与代理集成。
4. **持续学习**：每次交互都会被存储，以改善未来的响应。

## 结论

通过将 LlamaIndex 与 Mem0 集成，您可以构建个性化代理，该代理可以在与代理交互时维护上下文，并提供量身定制的建议和帮助。<卡组列={2}>
  <Card title="LlamaIndex Multiagent Cookbook" icon="brain" href="/cookbooks/frameworks/llamaindex-multiagent">
    使用 LlamaIndex 和 Mem0 构建多代理系统
  </卡>
  <Card title="LlamaIndex ReAct Cookbook" icon="bolt" href="/cookbooks/frameworks/llamaindex-react">
    使用 LlamaIndex 创建 ReAct 代理
  </卡>
</卡组>
