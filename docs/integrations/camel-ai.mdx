---
title: Camel AI
description: "Plug Mem0 cloud memory into Camel's agents with the built‑in Mem0Storage."
partnerBadge: "Camel AI"
---

> 中文注释：本文件非代码内容已翻译为中文，代码块与配置示例保持原样。



# 骆驼人工智能集成

将 Camel 的代理框架连接到 Mem0，以便每个代理都可以通过最少的设置保留并调用跨会话的对话上下文。

<信息>
  **先决条件**
  - Mem0：`MEM0_API_KEY`（或自托管端点），`pip install mem0ai`
  - Camel AI：`pip installcamel-ai`（需要 Python 3.9+）
  - 可选：如果您运行 LLM 支持的代理，则 OpenAI API 密钥
</信息>

<注意>Camel 今天提供了 Python SDK。 TypeScript 路径尚不可用。</Note>

## 配置凭据

<标签>
  <标签标题=“Mem0”>
<步骤>
<Step title="导出您的 API 密钥">```bash
export MEM0_API_KEY="sk-..."
```
</步骤>
<Step title="(自托管)指向您的 Mem0 API">```bash
export MEM0_BASE_URL="https://your-mem0-domain"
```
</步骤>
</步骤>
  </标签>
  <标签标题=“骆驼”>
<步骤>
<Step title="安装带有 Mem0 依赖项的 Camel">```bash
pip install "camel-ai>=0.2.0" mem0ai
```
</步骤>
<Step title="(可选)添加您的模型凭据">```bash
export OPENAI_API_KEY="sk-openai..."
```
</步骤>
</步骤>
  </标签>
</标签>

<提示>
  Mem0Storage 自动读取“MEM0_API_KEY”。仅当您需要覆盖环境时才显式传递“api_key”。
</提示>

## 将 Mem0 连接到 Camel 代理

<步骤>
<Step title="创建 Mem0 支持的内存存储">```python
import os
from camel.storages import Mem0Storage

mem0_store = Mem0Storage(
    api_key=os.environ.get("MEM0_API_KEY"),
    agent_id="travel_agent",
    user_id="alice",
    metadata={"source": "camel-demo"},
)
```
</步骤>
<Step title="将其附加到 Camel 内存">```python
from camel.memories import ChatHistoryMemory, ScoreBasedContextCreator
from camel.utils import OpenAITokenCounter
from camel.types import ModelType

memory = ChatHistoryMemory(
    context_creator=ScoreBasedContextCreator(
        token_counter=OpenAITokenCounter(ModelType.GPT_4O_MINI),
        token_limit=1024,
    ),
    storage=mem0_store,
    agent_id="travel_agent",
)
```
</步骤>
<Step title="让您的代理读写 Mem0">```python
from camel.agents import ChatAgent
from camel.messages import BaseMessage

agent = ChatAgent(
    system_message=BaseMessage.make_assistant_message(
        role_name="Agent",
        content="You are a helpful travel assistant. Reuse stored memories."
    )
)

agent.memory = memory

response = agent.step(
    BaseMessage.make_user_message(
        role_name="User",
        content="I prefer boutique hotels in Paris."
    )
)

print(response.msgs[0].content)
```
</步骤>
</步骤>

<信息图标=“检查”>
  运行“python camel_mem0_demo.py”（或上面 REPL 中的代码片段）。您应该看到代理响应并且内存保留到 Mem0。使用新提示重新运行应包括存储的首选项。
</信息>

## 验证集成

- Mem0 仪表板显示 `agent_id=travel_agent` 和 `user_id=alice` 下的新记忆。
- `mem0_store.load()` 返回您刚刚写入的记录。
- Camel 代理在后续运行中回复参考先前的用户首选项。

## 故障排除

- **缺少 MEM0_API_KEY** — 设置 `export MEM0_API_KEY="sk-..."` 或将 `api_key` 传递到 `Mem0Storage` 中。
- **没有返回记忆** — 确保查询中的 `agent_id`/`user_id` 与您在编写时使用的内容匹配。
- **Mem0 出现网络错误** — 如果是自托管，请将“MEM0_BASE_URL”设置为您的部署 URL。<卡组列={2}>
  <卡片
    title="Mem0 中的内存类型"
    description="为您的 Camel 代理选择聊天历史记录和语义搜索。"
    图标=“闪闪发光”
    href="/核心概念/内存类型"
  />
  <卡片
    title="接下来试试浪链"
    description="将同一个 Mem0 项目连接到 LangChain 工作流程中。"
    图标=“火箭”
    href="/integrations/langchain"
  />
</卡组>
