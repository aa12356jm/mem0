---
title: LangGraph
description: "Build customer support agents using LangGraph for conversation flow and Mem0 for personalized responses."
---

> 中文注释：本文件非代码内容已翻译为中文，代码块与配置示例保持原样。



使用 LangGraph 实现对话流，使用 Mem0 实现内存保留，构建个性化客户支持 AI 代理。这种集成可实现上下文感知和高效的支持体验。

## 概述

在本指南中，我们将创建一个客户支持 AI 代理，该代理可以：
1.使用LangGraph管理会话流程
2. 利用 Mem0 存储和检索过去交互的相关信息
3. 根据用户历史记录提供个性化响应

## 设置和配置

安装必要的库：```bash
pip install langgraph langchain-openai mem0ai python-dotenv
```
导入所需模块并设置配置：

<Note>记得从<a href="https://app.mem0.ai?utm_source=oss&utm_medium=integration-langgraph" rel="nofollow">Mem0 Platform</a>获取Mem0 API密钥。</Note>```python
from typing import Annotated, TypedDict, List
from langgraph.graph import StateGraph, START
from langgraph.graph.message import add_messages
from langchain_openai import ChatOpenAI
from mem0 import MemoryClient
from langchain_core.messages import SystemMessage, HumanMessage, AIMessage
from dotenv import load_dotenv

load_dotenv()

# Configuration
# OPENAI_API_KEY = 'sk-xxx'  # Replace with your actual OpenAI API key
# MEM0_API_KEY = 'your-mem0-key'  # Replace with your actual Mem0 API key

# Initialize LangChain and Mem0
llm = ChatOpenAI(model="gpt-4")
mem0 = MemoryClient()
```
## 定义状态和图

设置会话状态和 LangGraph 结构：```python
class State(TypedDict):
    messages: Annotated[List[HumanMessage | AIMessage], add_messages]
    mem0_user_id: str

graph = StateGraph(State)
```
## 创建聊天机器人功能

定义客户支持人工智能代理的核心逻辑：```python
def chatbot(state: State):
    messages = state["messages"]
    user_id = state["mem0_user_id"]

    try:
        # Retrieve relevant memories
        memories = mem0.search(messages[-1].content, filters={"user_id": user_id})
        
        # Handle dict response format
        memory_list = memories['results']

        context = "Relevant information from previous conversations:\n"
        for memory in memory_list:
            context += f"- {memory['memory']}\n"

        system_message = SystemMessage(content=f"""You are a helpful customer support assistant. Use the provided context to personalize your responses and remember user preferences and past interactions.
{context}""")

        full_messages = [system_message] + messages
        response = llm.invoke(full_messages)

        # Store the interaction in Mem0
        try:
            interaction = [
                {
                    "role": "user",
                    "content": messages[-1].content
                },
                {
                    "role": "assistant", 
                    "content": response.content
                }
            ]
            result = mem0.add(interaction, user_id=user_id)
            print(f"Memory saved: {len(result.get('results', []))} memories added")
        except Exception as e:
            print(f"Error saving memory: {e}")
            
        return {"messages": [response]}
        
    except Exception as e:
        print(f"Error in chatbot: {e}")
        # Fallback response without memory context
        response = llm.invoke(messages)
        return {"messages": [response]}
```
## 设置图形结构

使用适当的节点和边配置 LangGraph：```python
graph.add_node("chatbot", chatbot)
graph.add_edge(START, "chatbot")
graph.add_edge("chatbot", "chatbot")

compiled_graph = graph.compile()
```
## 创建对话运行器

实现一个功能来管理对话流：```python
def run_conversation(user_input: str, mem0_user_id: str):
    config = {"configurable": {"thread_id": mem0_user_id}}
    state = {"messages": [HumanMessage(content=user_input)], "mem0_user_id": mem0_user_id}

    for event in compiled_graph.stream(state, config):
        for value in event.values():
            if value.get("messages"):
                print("Customer Support:", value["messages"][-1].content)
                return
```
## 主交互循环

设置用户交互的主程序循环：```python
if __name__ == "__main__":
    print("Welcome to Customer Support! How can I assist you today?")
    mem0_user_id = "alice"  # You can generate or retrieve this based on your user management system
    while True:
        user_input = input("You: ")
        if user_input.lower() in ['quit', 'exit', 'bye']:
            print("Customer Support: Thank you for contacting us. Have a great day!")
            break
        run_conversation(user_input, mem0_user_id)
```
## 主要特点

1. **内存集成**：使用 Mem0 存储和检索过去交互的相关信息。
2. **个性化**：根据用户历史记录提供上下文感知响应。
3. **灵活的架构**：LangGraph结构允许轻松扩展会话流程。
4. **持续学习**：每次交互都会被存储，以改善未来的响应。

## 结论

通过将 LangGraph 与 Mem0 集成，您可以构建个性化的客户支持 AI 代理，该代理可以维护交互中的上下文并提供个性化帮助。

<卡组列={2}>
  <Card title="LangChain Integration" icon="link" href="/integrations/langchain">
    使用 LangChain 和 Mem0 构建会话代理
  </卡>
  <Card title="CrewAI Integration" icon="users" href="/integrations/crewai">
    使用 CrewAI 创建多代理系统
  </卡>
</卡组>
